Build information
    project: ${project.parent.name}
    module: ${project.name}
    version: ${revision}
    built on: ${timestamp}
    built by: ${user.name}
    java version: ${maven.compiler.target}